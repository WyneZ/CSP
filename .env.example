# --- Database (local docker-compose postgres) ---
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/csp_erp

# --- API (apps/api) ---
API_PORT=4000
JWT_SECRET=change-me-in-real-env

# --- Web (apps/web) ---
NEXT_PUBLIC_API_URL=http://localhost:4000
